<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThemeStylesIcons"
        tools:targetApi="31">
        <activity
            android:name=".CompeticoesActivity"
            android:exported="false"
            android:parentActivityName=".ClubeActivity"/>
        <activity
            android:name=".SobreActivity"
            android:exported="false"
            android:parentActivityName=".ClubesActivity" />
        <activity
            android:name=".ClubesActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ClubeActivity"
            android:exported="false"
            android:parentActivityName=".ClubesActivity"></activity>
    </application>

</manifest>